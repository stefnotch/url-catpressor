{
  "name": "url-catpressor",
  "version": "0.0.0",
  "scripts": {
    "start": "vite",
    "dev": "vite --open",
    "build": "vue-tsc --noEmit && vite build",
    "preview": "vite preview --port 5050",
    "deploy": "vue-tsc --noEmit && vite build --base=/url-catpressor/ && node utils/publish.js",
    "typecheck": "vue-tsc --noEmit"
  },
  "dependencies": {
    "vue": "^3.2.33"
  },
  "devDependencies": {
    "@types/node": "^16.11.27",
    "@vitejs/plugin-vue": "^2.3.1",
    "@vue/tsconfig": "^0.1.3",
    "gh-pages": "^3.2.3",
    "typescript": "~4.6.3",
    "vite": "^2.9.5",
    "vue-tsc": "^0.34.7"
  }
}
